# 09: Database table teardown

**What to build:** The checklist table leaves the schema: a new migration drops the per-approver checklist table, and the schema definition is deleted. The table is already empty (live rows were purged), and nothing in the code writes to it anymore.

**Blocked by:** 08 — Server teardown

**Status:** resolved

- [x] A new migration drops the checklist table; the migration file that originally created it is untouched.
- [x] The schema module for the table and its export in the schema index are deleted.
- [x] The app develops and deploys cleanly against the migrated database with no orphaned schema references.
- [x] Full suite green; typecheck, lint, and build clean.

## Comments

Implemented 2026-08-30:

- **Migration**: `drizzle/0006_safe_zemo.sql` is a single `DROP TABLE "submission_approver_checklists" CASCADE;` generated by `drizzle-kit generate` after the schema removal — it contains nothing else, and `drizzle/0005_slim_catseye.sql` (the creating migration) is untouched. The generated meta (`_journal.json` entry 6, `0006_snapshot.json`) is committed alongside; the snapshot carries zero references to the table. The table dropped with its two indexes and both FK constraints (owned objects, dropped with the table).
- **Schema removal**: `db/schema/approver-checklists.ts` (`submissionApproverChecklists`) deleted via `git rm`; the `export * from './approver-checklists'` line removed from `db/schema/index.ts`. Grep over the code tree (`server`, `shared`, `db`, `components`, `pages`, `composables`, `tests`) shows no remaining import of the table or module — only historical records name it (ADR-0003, the spec, ticket 01/08).
- **Migrated database**: confirmed `submission_approver_checklists` was empty (0 rows) before the drop, journal already applied through 0005 (entries 1–6). Ran `pnpm db:migrate` against the configured database — 0006 applied, `to_regclass('public.submission_approver_checklists')` now returns null, journal closed at 7 entries. The app's queries reference the table nowhere, so the drop leaves no dangling server code.
- **Verification**: full suite 229 green (unchanged from 08's baseline — no tests touched the schema module), `pnpm typecheck` exit 0, `pnpm lint` 0 errors (3 pre-existing `v-html` warnings in files untouched here), `nuxt build` clean.