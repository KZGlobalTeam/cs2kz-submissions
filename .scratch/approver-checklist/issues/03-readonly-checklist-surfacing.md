# 03: Read-only checklist surfacing after review

**What to build:** The same Approver checklist section shown read-only once the submission has left review. After the submission is approved or rejected, the owning plain approver can still see the ticks and note they saved — controls inert, no save behavior — so their private verification record survives for reference. If the approver never saved anything, the section is hidden entirely rather than shown as an empty box. Visibility stays strictly private: lead approvers and the submitting mapper never see the section in any mode, and the read-only view never touches another approver's state.

**Blocked by:** 02 — Editable checklist section beside the vote form

**Status:** ready-for-agent

- [x] When the submission has left review (approved or rejected), the owning plain approver still sees their checklist and note, rendered read-only — no editable checkboxes, no note editing, no save indicator or save behavior.
- [x] The read-only section is hidden entirely when the approver never saved anything (no ticks and no note).
- [x] The read-only view obeys the same porting rule as the editable one: the porting group renders only when the submission is a port.
- [x] Placement mirrors where the editable section sits during review, keeping the two-column (desktop) / stacked (mobile) layout.
- [x] Lead approvers and the submitting mapper never see the section in any mode, and the read-only rendering never fetches or displays state belonging to another approver.

## Comments

Implemented and verified 2026-08-26:

- New `ApproverChecklistReadonly.vue`: for approved/rejected submissions the owning plain approver sees their saved ticks and Approver note as a read-only card — disabled `UCheckbox` buttons carrying the saved ticks (`seedChecklistGroups` normalization, per-index), the note rendered as static text (no textarea), no save indicator, no save machinery (no debounce, no `flush`, no PUT). Group collapsibles mirror the editable section (expanded by default); the porting group renders only when the submission's recorded `isPort` fact is true (same `visibleRuleGroups` shared with the editable section and the mapper dialog; saved ticks for non-rendered groups are dropped by the shared seed).
- Hidden entirely when never saved: the section fetches `GET /api/submissions/[id]/approver-checklist` (the same owned-row, plain-approver-gated endpoint — no other approver's state can surface) and renders only when the row carries content. New pure helper `hasSavedContent(ticks, note)` (any tick set, or a non-empty note) — both a missing row and an explicit reset-to-nothing row (all unchecked, note cleared) render nothing, so a finished submission never shows an empty box. Content is judged on the *port-filtered seeded* view, so ticks that could never render (e.g. a foreign PUT storing `porting` ticks on a non-port row) can't keep the card visible as an empty box (spec-review P2, fixed).
- Page wiring: once `!isPending`, a plain approver's main content becomes the same `grid gap-6 lg:grid-cols-2` the editable card used during review — courses left, sticky read-only card right on desktop (sticky at `lg:top-6`), stacked on mobile. The section emits `loaded` with its resolved visibility; the side column (`v-show`) and the `lg:grid-cols-2` class are applied only when there is something to show, so a never-saved approver gets courses full-width with no empty column.
- Privacy/roles: the branch is reached only for `isPlainApprover` (`approver` and not `lead_approver`) on non-pending submissions; lead approvers and mappers keep the existing single-column courses view and no checklist request ever fires on their pages.
- Error path: a failed load surfaces a muted error box (mirroring the editable section) instead of masquerading as "never saved" — never an empty box.
- Pure-function seam: `hasSavedContent` added to `components/review/approver-checklist-state.ts` with 6 new tests in `tests/server/utils/approver-checklist-state.spec.ts` (never-saved, empty checklist, reset-to-nothing row, ticked rule, note-only, whitespace note). Full suite 81 passing; typecheck, lint (0 errors; the 3 `v-html` warnings are the pre-existing deferred pattern shared with the editable section and dialog), and build clean.
- Browser battery (existing dev server :11451 + minted plain-approver/lead/mapper sessions, real API calls, all test data cleaned up afterwards): 4 submissions finalized via the real decision API — lead role sees no section on approved pages; plain approver sees ticks+note read-only on a non-port approval (no porting group, checkboxes disabled, no save indicator); port approval shows the "Rules for Porting" group with the saved note and no ticks; a never-saved approval shows no section and courses stay full-width (no empty column); a different approver's saved checklist is never visible; layout verified at 1440px (two side-by-side 556px columns, card sticky) and 390px (single column, card stacked below courses); roleless mapper sees no section; pending-submission editable section regression-checked (25 checkboxes, note textarea, two-column). Submissions reverted to pending, checklist rows and final-filter rows deleted, minted session removed, roles restored.
- Reviewed via code-review skill (parallel Standards + Spec reviewer sub-agents): Standards — 0 hard violations, 3 P2 judgement calls (template duplication vs the editable section, deferred as a pattern; param-only route reuse keeps the one-shot fetch stale, unreachable via current navigation; helper shape fine). Spec — all 5 acceptance bullets satisfied; P2 on the raw-vs-seeded content check fixed as noted above; P2 on the load-error card accepted (mirrors the editable section's error affordance; a failed load must not masquerade as "never saved").