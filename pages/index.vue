<script setup lang="ts">
// The bare path has no game context: every page lives under its game
// segment, so `/` is simply the well-defined default — CS2.
definePageMeta({
  redirect: '/cs2',
})
</script>
